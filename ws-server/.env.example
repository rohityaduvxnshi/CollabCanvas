# ws-server environment (Fly.io). Copy to `.env` for local dev.

# Same Neon Postgres connection string as the web app (used from Phase 3).
DATABASE_URL=

# Must be byte-for-byte identical to the web app's WS_JWT_SECRET (used from Phase 4).
WS_JWT_SECRET=

# Port the server listens on.
PORT=4000
